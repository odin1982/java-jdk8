jdk8:
   variables locales:
      rules:
        - Las variables locales deben ser inicializadas
   ArrayList:
      metodos:
         set:
            declaration: set(int index, E element)
            definicion:  Replaces the element at the specified position in this list with the specified element.
         remove:
            declaration:  remove(int index)
            definicion:   Removes the element at the specified position in this list.
   wildcard:
      definition: The * is a wildcard y obtiene todas las clases en un paquete.
   packages:
      java.lang: Este paquete es especial ya que es automaticamnete importado por Java.
   paquetes redundantes:
      definition: son aquellos paquetes que no necesitamos agregar en los imports como los de java.lang y clases que s eencuentren en
      el mismo paquete o visibilidad.
   constructors:
      definition: Es un tipo de metodo especial que crea un objeto.
      rules:
         - debe tener el mismo nombre de la clase
         - No debe tener tipo de retorno
   
   
   

   
       

   PUNTOS DEBILES DETECTADOS:
      temas:
         - jerarquia de excepciones
   